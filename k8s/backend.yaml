apiVersion: v1
kind: ConfigMap
metadata:
  name: kokoko-config
  namespace: kokoko
data:
  CONTAINER_NAMESPACE: "kokoko-containers"
  BACKEND_NAMESPACE:   "kokoko"
  CONTAINER_IMAGE:     "kokoko/ubuntu-ttyd:latest"
  DATABASE_URL:        "sqlite+aiosqlite:////app/data/kokoko.db"
  TTYD_PORT:           "7681"
  MAX_CONTAINERS_PER_USER: "5"
---
# Store the SECRET_KEY in a Kubernetes Secret (base64-encoded).
# Replace the value with: echo -n "your-secret" | base64
apiVersion: v1
kind: Secret
metadata:
  name: kokoko-secrets
  namespace: kokoko
type: Opaque
stringData:
  SECRET_KEY: "CHANGE-ME-generate-with-openssl-rand-hex-32"
---
# PersistentVolumeClaim for SQLite database storage.
# For production, consider using PostgreSQL instead.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kokoko-db-pvc
  namespace: kokoko
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kokoko-backend
  namespace: kokoko
  labels:
    app: kokoko-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kokoko-backend
  template:
    metadata:
      labels:
        app: kokoko-backend
    spec:
      serviceAccountName: kokoko-backend
      containers:
        - name: backend
          image: kokoko/backend:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
              protocol: TCP
          envFrom:
            - configMapRef:
                name: kokoko-config
            - secretRef:
                name: kokoko-secrets
          volumeMounts:
            - name: db-storage
              mountPath: /app/data
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: db-storage
          persistentVolumeClaim:
            claimName: kokoko-db-pvc
---
# NodePort Service — accessible via <NodeIP>:30080
apiVersion: v1
kind: Service
metadata:
  name: kokoko-backend-svc
  namespace: kokoko
  labels:
    app: kokoko-backend
spec:
  selector:
    app: kokoko-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
      protocol: TCP
  type: NodePort
