apiVersion: v1
kind: Namespace
metadata:
  name: kuboco
  labels:
    kubernetes.io/metadata.name: kuboco
# Per-user namespaces (kuboco-user-{id}) are created dynamically by the backend
# when a user first spawns a container. No static manifest needed.
